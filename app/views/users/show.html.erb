<%= render 'books/list', books:@book %>
<div class= 'col-md-8 offset-md-1'>
<h2>Books</h2>
<table class='table'>
<thead>
<tr>
<th></th>
<th>Title</th>
<th>Opinion</th>
<th colspan="3"></th>
</tr>
</thead>
<tbody>
<tr>
<td>
<% @books.each do |book| %>
<%= link_to user_path(@user.id) do %>
<%= image_tag @user.get_profile_image(60,60) %></td>
<% end %>
<td><%= book.title %></td>
<td><%= book.body %></td>
<% end %>
</tr>
</tbody>
</table>
</div>
